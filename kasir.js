const cassier = (restoName, cassierName, items) => {
  const date = new Date().toISOString();
  items.map((item) => {
    if(item.le)
  });
  const result = `
  ${restoName}
  Tanggal : ${date}
  Nama Kasir : ${cassierName}
  ================================
  `;
};
